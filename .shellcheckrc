# ShellCheck Configuration for Claude Enhancer v5.4.0
# Purpose: Define linting rules for Bash scripts

# Enable additional checks
enable=all

# Disable specific checks
# SC1090: Can't follow non-constant source
disable=SC1090

# SC1091: Not following sourced files
disable=SC1091

# SC2034: Variable appears unused (often used in sourced files)
# disable=SC2034

# SC2155: Declare and assign separately to avoid masking return values
# (we handle this carefully in our code)
# disable=SC2155

# Shell dialect
shell=bash

# Severity threshold (error, warning, info, style)
severity=style

# External sources (paths to check for sourced files)
source-path=SCRIPTDIR

# Color output
color=auto
